#include "lists.h"

/**
 * print_listint - prints the elements of a linked list
 * @h: listint_t type
 * Return: size_t type
 */
size_t print_listint(const listint_t *h)
{
  size_t count = 0;

  while (h)
    {
      printf("%d\n", h->n);
      count++;
      h = h->next;
    }
  return (count);
}
