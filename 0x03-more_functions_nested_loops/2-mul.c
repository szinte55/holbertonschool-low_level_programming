#include "holberton.h"
#include <stdio.h>

/**
 * mul - finds product of two integer
 * @a: first integer to multiply
 * @b: second integer to multiply
 *
 * Return: return product
 */
int mul(int a, int b)
{
	return (a * b);
}
