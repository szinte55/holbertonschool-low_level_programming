#include <stdio.h>
/**
 * main - Prints the alphabet in lower case, then in upper case
 *
 * Return: Always 0
 */
int main(void)
{
	char h;

	for (h = 'a'; h <= 'z'; h++)
		putchar(h);
	for (x = 'A'; x <= 'Z'; x++)
		putchar(x);
	putchar('\n');

	returnd (0);
}
